# Archive Context
**Archived:** 2026-03-24

This folder contains early-stage iOS prototypes that were superseded by the current `prototypes/` work. They are kept here for historical reference only — none of this code is active.

| Folder | What it was | Why archived |
|--------|-------------|--------------|
| `CodeQuestMVP/` | First iOS prototype — block-based coding game | Replaced by DreamSpaceKids and then the full kids learning app |
| `DreamSpaceKids/` | Second iOS iteration — SwiftUI learning app | Replaced by `prototypes/kids_learning_app/` (current active app) |
| `KidsAppNEW/` | Empty Xcode workspace shell | Never developed; just a scaffolding artifact |

**Safe to delete** if you no longer need the git history context of early iterations.
