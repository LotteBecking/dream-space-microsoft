import SwiftUI

enum Theme {
    static let background = Color("DreamSpaceBackground", bundle: nil)
    static let card = Color("DreamSpaceCard", bundle: nil)
    static let accent = Color("DreamSpaceAccent", bundle: nil)
    static let highlight = Color("DreamSpaceHighlight", bundle: nil)
}
